import React from 'react';
import AnimatorsComponent from '../components/Animators';

const Animators: React.FC = () => {
  return <AnimatorsComponent isFullPage={true} />;
};

export default Animators;
